bleak>=0.21.0
websockets>=12.0

# Dev / CI
pytest>=7.0
pytest-asyncio>=0.21.0
flake8>=6.0
mypy>=1.9.0

# Optional for parquet output in collector tests
pyarrow>=10.0.0

# Coverage tool used by CI
coverage>=7.2.0
