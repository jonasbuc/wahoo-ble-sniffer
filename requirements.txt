bleak>=0.21.0
websockets>=12.0

# Live Analytics pipeline
fastapi>=0.104,<1
uvicorn[standard]>=0.24,<1
pydantic>=2,<3
requests>=2.31,<3
streamlit>=1.33,<2
numpy>=1.26,<2

# Required by FastAPI/Starlette StaticFiles for async file delivery
# under a real uvicorn server (questionnaire + system_check UIs)
aiofiles>=23.0
sqlalchemy>=2.0
# HTTP client used by preflight.py health checks and internal service probes
httpx>=0.27,<1

# Dev
pytest>=7.0
pytest-asyncio>=0.21.0
pytest-cov>=4.0

# Optional for parquet output in collector tests
pyarrow>=10.0.0

# Used by live_analytics dashboard
pandas>=2.0
