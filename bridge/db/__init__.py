# db package — database utilities for the VRS collector
#
# Sub-packages:
#   db.sqlite  — SQLite helpers (views, export, validate, pretty-print)
#   db.mssql   — MSSQL schema + JSONL → MSSQL flush pipeline
