# db.sqlite — SQLite utilities for the VRS collector
#
# Modules:
#   create_readable_views  — add human-readable timestamp views
#   export_readable_views  — export views to CSV / Parquet
#   pretty_dump_db         — pretty-print DB contents to stdout
#   validate_db            — validate data integrity (ranges, JSON, etc.)
